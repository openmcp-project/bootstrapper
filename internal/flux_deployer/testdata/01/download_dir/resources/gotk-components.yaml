apiVersion: apps/v1
kind: Deployment
metadata:
  name: source-controller
  namespace: {{ .Values.namespace }}
spec:
  template:
    spec:
      containers:
        - image: {{ index .Values.images "fluxcd-source-controller" }}
